# To use, rename this file to ~/.pymolrc

# deprecated
# run ~/python_scripts/pymol_scripts/all_commands.py

# note that the following standard syntax will work as long as the package is defined correctly and Python knows where to find it (the PYTHONPATH environment variable should indicate the base directory where the user's additional Python packages are located) -Ashworth Aug 2008

# the following method /may/ work if you do not want to set PYTHONPATH
import sys
sys.path.append("~/python_scripts/")

from pymol_scripts import *
